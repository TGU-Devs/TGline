#!/usr/bin/env bash
set -euo pipefail

project_root="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
source "$project_root/bin/load-local-env"

cd "$project_root/backend"
exec bundle exec rails server -b 0.0.0.0 -p 3001
